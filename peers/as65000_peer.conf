protocol bgp as65000_peer {
    local as OWNAS;
    source address OWNIPv6_S;
    # multihop;
    neighbor;
    ipv6 {
        import filter import_filter;
        export filter export_filter;
    };
    password "password";
    graceful restart;

    hold time 90;
    keepalive time 15;
}